f :: (Int,Int,Int,Int) -> Int
f (x,y,x,x) = x

g :: (Int,Int,Int,Int,Int) -> Int
g (x,y,z,z,x) = x + y + z

f :: (Int) -> Int
f (x) = x

f :: (Int,Int) -> Int
f (s,s) = s

g :: (Int) -> Int
g (x) = x * z

main = f (2)