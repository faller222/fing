program ejemplo5;
var x : integer;
    y : integer;
    w : boolean;
    y : boolean;
begin 
   writeln(w);
   readln(z);
   readln(w);
   readln(t);
   for i := 1 to 10 do
   begin
      w := w + z
   end
end.
